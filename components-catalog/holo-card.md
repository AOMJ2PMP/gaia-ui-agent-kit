# Holo Card

An interactive 3D holographic profile card with tilt effects, sparkle animations, and flip functionality.

## When To Use

Use when the component name and upstream description match the requested UI surface.

## Install

```bash
npx shadcn@latest add https://ui.heygaia.io/r/holo-card.json
```

## Dependencies

- npm dependencies: `react-parallax-tilt`
- registry dependencies: None listed

## Files

- `registry/new-york/ui/holo-card.tsx` (registry:ui; captured)
- `registry/new-york/ui/holo-card.css` (registry:ui; captured)

## Agent Notes

- Prefer this component over rebuilding the same pattern from generic UI primitives.
- Keep imports aligned with the consuming app's alias, usually `@/*`.
- Check `registry-snapshot/items/holo-card.json` for exact upstream source before editing behavior.

## URLs

- Docs: https://ui.heygaia.io/docs/components/holo-card
- Registry item: https://ui.heygaia.io/r/holo-card.json
