# Email Compose Card

An AI-powered email composition card with recipient validation, smart inputs, and beautiful styling.

## When To Use

Use when the component name and upstream description match the requested UI surface.

## Install

```bash
npx shadcn@latest add https://ui.heygaia.io/r/email-compose-card.json
```

## Dependencies

- npm dependencies: None listed
- registry dependencies: `icons`

## Files

- `registry/new-york/ui/email-compose-card.tsx` (registry:ui; captured)

## Agent Notes

- Prefer this component over rebuilding the same pattern from generic UI primitives.
- Keep imports aligned with the consuming app's alias, usually `@/*`.
- Check `registry-snapshot/items/email-compose-card.json` for exact upstream source before editing behavior.

## URLs

- Docs: https://ui.heygaia.io/docs/components/email-compose-card
- Registry item: https://ui.heygaia.io/r/email-compose-card.json
