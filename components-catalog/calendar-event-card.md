# Calendar Event Card

A styled card for displaying calendar events with color indicators, status states, and action buttons.

## When To Use

Calendar reminders, meeting cards, agenda views, and scheduling workflows.

## Install

```bash
npx shadcn@latest add https://ui.heygaia.io/r/calendar-event-card.json
```

## Dependencies

- npm dependencies: `lucide-react`
- registry dependencies: None listed

## Files

- `registry/new-york/ui/calendar-event-card.tsx` (registry:ui; captured)

## Agent Notes

- Prefer this component over rebuilding the same pattern from generic UI primitives.
- Keep imports aligned with the consuming app's alias, usually `@/*`.
- Check `registry-snapshot/items/calendar-event-card.json` for exact upstream source before editing behavior.

## URLs

- Docs: https://ui.heygaia.io/docs/components/calendar-event-card
- Registry item: https://ui.heygaia.io/r/calendar-event-card.json
