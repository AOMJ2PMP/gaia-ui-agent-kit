# Nested Menu

A hover-activated nested menu component for multi-level navigation with smooth animations.

## When To Use

Use when the component name and upstream description match the requested UI surface.

## Install

```bash
npx shadcn@latest add https://ui.heygaia.io/r/nested-menu.json
```

## Dependencies

- npm dependencies: `@radix-ui/react-popover`
- registry dependencies: None listed

## Files

- `registry/new-york/ui/nested-menu.tsx` (registry:ui; captured)

## Agent Notes

- Prefer this component over rebuilding the same pattern from generic UI primitives.
- Keep imports aligned with the consuming app's alias, usually `@/*`.
- Check `registry-snapshot/items/nested-menu.json` for exact upstream source before editing behavior.

## URLs

- Docs: https://ui.heygaia.io/docs/components/nested-menu
- Registry item: https://ui.heygaia.io/r/nested-menu.json
