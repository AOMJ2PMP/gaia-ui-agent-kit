# Wave Spinner

A beautiful animated loading spinner with wave effects, multiple patterns, colors, and highly customizable animations.

## When To Use

Use when the component name and upstream description match the requested UI surface.

## Install

```bash
npx shadcn@latest add https://ui.heygaia.io/r/wave-spinner.json
```

## Dependencies

- npm dependencies: `class-variance-authority`
- registry dependencies: None listed

## Files

- `registry/new-york/ui/wave-spinner.tsx` (registry:ui; captured)
- `registry/new-york/ui/wave-spinner.css` (registry:ui; captured)

## Agent Notes

- Prefer this component over rebuilding the same pattern from generic UI primitives.
- Keep imports aligned with the consuming app's alias, usually `@/*`.
- Check `registry-snapshot/items/wave-spinner.json` for exact upstream source before editing behavior.

## URLs

- Docs: https://ui.heygaia.io/docs/components/wave-spinner
- Registry item: https://ui.heygaia.io/r/wave-spinner.json
