# Raised Button

A beautiful 3D raised button with customizable colors and dynamic text contrast.

## When To Use

Primary CTAs with GAIA's tactile accent treatment and dynamic text contrast.

## Install

```bash
npx shadcn@latest add https://ui.heygaia.io/r/raised-button.json
```

## Dependencies

- npm dependencies: `class-variance-authority`
- registry dependencies: None listed

## Files

- `registry/new-york/ui/raised-button.tsx` (registry:ui; captured)
- `lib/utils/colorUtils.ts` (registry:lib; captured)

## Agent Notes

- Prefer this component over rebuilding the same pattern from generic UI primitives.
- Keep imports aligned with the consuming app's alias, usually `@/*`.
- Check `registry-snapshot/items/raised-button.json` for exact upstream source before editing behavior.

## URLs

- Docs: https://ui.heygaia.io/docs/components/raised-button
- Registry item: https://ui.heygaia.io/r/raised-button.json
