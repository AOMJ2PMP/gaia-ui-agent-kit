# Navbar Menu

A beautiful animated dropdown menu for navigation with support for icons, descriptions, and custom layouts.

## When To Use

Use when the component name and upstream description match the requested UI surface.

## Install

```bash
npx shadcn@latest add https://ui.heygaia.io/r/navbar-menu.json
```

## Dependencies

- npm dependencies: `motion`, `next`
- registry dependencies: `raised-button`

## Files

- `registry/new-york/ui/navbar-menu.tsx` (registry:ui; captured)

## Agent Notes

- Prefer this component over rebuilding the same pattern from generic UI primitives.
- Keep imports aligned with the consuming app's alias, usually `@/*`.
- Check `registry-snapshot/items/navbar-menu.json` for exact upstream source before editing behavior.

## URLs

- Docs: https://ui.heygaia.io/docs/components/navbar-menu
- Registry item: https://ui.heygaia.io/r/navbar-menu.json
